name : CI Workflow
on :
    push :
        branches :
            - "coding-branch"
    pull_request :
        branches :
            - "main"

jobs :
    build :
        runs-on : ubuntu-latest
        steps :
            - name: Checks out code
              uses: actions/checkout@v2

            - name: Checks out python
              uses : actions/setup-python@v2
              with : 
                       python-version: 3.x
            
            - name: Install dependencies 
              run: pip install -r requirements.txt
             
            - name: Run tests
              run: python manage.py test analysis.tests.m

  
